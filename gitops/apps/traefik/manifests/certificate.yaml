apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: demo-quezadasubiabre-com
  namespace: traefik
spec:
  secretName: traefik-default-tls
  dnsNames:
    - demo.quezadasubiabre.com
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt-prod
