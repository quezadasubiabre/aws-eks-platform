apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: api-quezadasubiabre-com
  namespace: traefik
spec:
  secretName: traefik-default-tls
  dnsNames:
    - api.quezadasubiabre.com
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt-prod
