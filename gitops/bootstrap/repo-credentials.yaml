apiVersion: v1
kind: Secret
metadata:
  name: repo-aws-eks-platform
  namespace: argocd
  labels:
    argocd.argoproj.io/secret-type: repository
stringData:
  type: git
  url: https://github.com/quezadasubiabre/aws-eks-platform.git
  username: ${GITHUB_USERNAME}
  password: ${GITHUB_TOKEN}
