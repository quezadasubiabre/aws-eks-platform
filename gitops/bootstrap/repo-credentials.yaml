apiVersion: v1
kind: Secret
metadata:
  name: repo-k8s-cloud-project
  namespace: argocd
  labels:
    argocd.argoproj.io/secret-type: repository
stringData:
  type: git
  url: https://github.com/quezadasubiabre/k8s-cloud-project.git
  username: ${GITHUB_USERNAME}
  password: ${GITHUB_TOKEN}
